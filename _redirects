# Cloudflare Pages redirects for Light in Silence
# Make Flask application the PRIMARY website

# Flask app should be the main website - redirect root to Flask
/ /NPO-SCA/ 200

# All main routes should go directly to Flask app
/* /NPO-SCA/:splat 200

# Static assets for Flask app
/static/* /NPO-SCA/static/:splat 200 